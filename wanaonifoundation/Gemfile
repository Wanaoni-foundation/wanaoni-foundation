source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
gem 'puma', '~> 5.0'
source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
gem 'date', '3.2.2'
gem 'websocket-driver', '0.7.5'
source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
gem 'date', '3.2.2'
gem 'websocket-driver', '0.7.5'

# Override dependencies to avoid conflict
gem 'mail', '~> 2.8.1'
gem 'net-imap', '~> 0.5.0'
source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
gem 'date', '3.2.2'
gem 'websocket-driver', '0.7.5'

# Override dependencies to avoid conflict
gem 'mail', '~> 2.8.1'
gem 'net-imap', '~> 0.5.0'

# Add dependency overrides
require 'bundler/inline'

gemfile(true) do
  gem 'actionmailbox', git: 'https://github.com/rails/rails.git'
  gem 'actioncable', git: 'https://github.com/rails/rails.git'
end
source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
gem 'websocket-driver', '~> 0.7.5'  # Updated version
source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
gem 'date', '3.2.2'  # Compatible version of date
source 'https://rubygems.org'

gem 'rails', '~> 6.1.0'
# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
